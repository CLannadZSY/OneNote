连接微软服务器出了问题，我们可以修改DNS来尝试更好的连接微软的服务器。
更改DNS：
- 打开 `控制面板` - `网络共享中心`，点击左侧的 `更改适配器` 设置。
- 右键点击本地连接，选择属性，双击 `Internet协议版本4 (TCP/IPv4)` ，在 `首选DNS服务器` 中输入 `4.2.2.1` ，在备用DNS服务器中输入 `4.2.2.2` ，确认。
- 再次尝试同步笔记，连接OneNote服务器，同步笔记本就成功了。

?> 微软官方推荐了两个DNS服务器地址： `4.2.2.1` 和 `4.2.2.2`
